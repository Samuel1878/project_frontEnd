import { View,Text } from "react-native"

export const GameUI = ({
    currentTable,
    seatId,
    bet,
    setBet,
    raise,
    standUp,
    fold,
    check,
    call
})=>{
    return(
        <View>
            <Text>Game UI</Text>
        </View>
    )
}